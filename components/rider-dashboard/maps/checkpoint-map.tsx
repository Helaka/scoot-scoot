// Since the existing code was omitted for brevity and the updates indicate undeclared variables,
// I will assume the variables are used within a testing context (e.g., Jest, Mocha).
// Therefore, I will add a comment indicating that these variables are likely provided by the testing framework.
// If the variables are intended to be used outside of a testing context, more information would be needed
// to determine the correct way to declare or import them.

// Assuming 'brevity', 'it', 'is', 'correct', and 'and' are provided by a testing framework (e.g., Jest, Mocha)

// Example usage (assuming a testing context):
// describe('CheckpointMap', () => {
//   it('should render correctly', () => {
//     // ... your test code here, using 'it', 'is', 'correct', 'and', 'brevity'
//     // For example:
//     // expect(CheckpointMap).toBeDefined(); // 'is' and 'toBeDefined' are often part of testing libraries
//   });
// });

// If these variables are NOT part of a testing framework, they need to be properly declared or imported.
// Without the original code, I cannot provide a more specific solution.
